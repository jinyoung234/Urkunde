import styled from "styled-components";

export const GoResultLinkWrapper = styled.div`
  margin-bottom: 3rem;
  a {
    font-size: 2rem;
    font-weight: 500;
    color: ${(props) => props.theme.textColor};
  }
`;
